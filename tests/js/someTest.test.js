describe('Demo some test', () => {
    it('This is one test', () => {
        let val = true;
        expect(val).toEqual(true);
    });

    it('This is other test', () => {
        let val = true;
        expect(val).toEqual(true);
    });
});
